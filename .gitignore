.venv/
.cache/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
data/raw/
data/processed/
*.ckpt
*.pt
*.pth
dist/
build/
*.egg-info/
.coverage
htmlcov/

# S1 working CSVs: the frozen parquet artefacts and their sha256 manifest are
# committed instead; part files are per-scenario checkpoints, not deliverables
simulation-results-ct2i/raw/*.csv
simulation-results-ct2i/raw/*_parts/
simulation-results-ct2i/raw/*.log

# return-package ZIPs are build artefacts rebuilt by scripts/build_return_package.py;
# the contents are tracked under simulation-results-ct2i/ and hashed in PACKAGE_SHA256.json
simulation-results-ct2i_*.zip
simulation-results-ct2i-repaired_*.zip

# Derived figure data: 178,772,105 bytes, over GitHub's hard 100MB per-file
# limit, so it was purged from git history on 2026-08-19 to make the branch
# pushable (GH001; see REPAIR_REPORT.md section 14). It is DERIVED, not raw:
# regenerate it byte-identically with
#   python3 scripts/run_sim1_figures.py      (reads the frozen raw outputs)
# and it still ships, checksummed, inside the return package ZIP. Do not re-add
# it to the repository.
simulation-results-ct2i/08_SIM1_FIGURE_DATA.csv

# macOS Finder metadata
.DS_Store

# Phase R return package: contains a 129 MB bundle and a 55 MB archive.
# Delivery artefacts, not source. Never commit (GH001 rejects >100 MB files).
return_phaseR_20260819/
