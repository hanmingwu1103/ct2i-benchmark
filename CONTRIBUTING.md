# Contributing
This repository is under private Stage 2 development. All changes are
integrated by a single writer after independent review (see the Stage 2
protocol). Run `pytest` and `ruff check` before proposing changes. Every
experiment must run from a validated config file; do not hardcode dataset,
seed, or budget changes in source.
